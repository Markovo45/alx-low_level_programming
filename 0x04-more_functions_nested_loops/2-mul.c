#include "main.h"

/**
 * mul - multiplies two integers.
 * @a: is the first argument for the function int mul
 * @b: is the second argument for the function int mul
 * Return: 0.
 */

int mul(int a, int b)
{
	int mul = (a * b);

	_putchar(sum + '0');
	_putchar('\n');
}


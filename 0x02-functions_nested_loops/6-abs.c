#include "main.h"
/**
 * _abs - prints the absolute value of an integer.
 * @int: the int for the argument of the function
 * Return: 0
 */
int _abs(int)
{
	int n;

	if (n > 0 || n == 0)
	{
		return (n);
	}
	else
	return (n * -1);
}

#include "main.h"

/**
 * swap_int - swabs the value of pointer.
 * @a: first integer argument
 * @b: Second integer argument.
 * Return: 0.
 */
int _strlen(char *s)
{
	int temporary = *a;

	*a = *b;
	*b = temporary;
}
